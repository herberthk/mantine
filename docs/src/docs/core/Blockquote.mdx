---
package: mantine-core
title: Blockquote
order: 1
slug: /core/blockquote/
category: 'typography'
description: 'Display quote with optional cite'
---

import { BlockquoteDemos } from '@demos/core';

# Blockquote

<PageHeader
  source="mantine-core/src/Blockquote/Blockquote.tsx"
  docs="core/Blockquote.mdx"
  package="@mantine/core"
/>

```tsx
import { Blockquote } from '@mantine/core';
```

## Usage

You can choose any color defined in <GatsbyLink to="/pages/theming/">theme</GatsbyLink>. By default Blockquote will use `theme.primaryColor` value.

<BlockquoteDemos.configurator />

## Configure icon

You can use any React node as icon by passing it to `icon` prop.
Note that component **does not control icon size** and you need to specify it manually on icon component for better fit.

You can use icons from any react icons library, for example:

- [radix icons](https://icons.modulz.app/) (used in mantine packages)
- [feather](https://feathericons.com/)
- [react-icons](https://react-icons.github.io/react-icons)

<BlockquoteDemos.icon />

By default blockquote uses quote icon, to disable it, set `icon` prop to null:

<BlockquoteDemos.noIcon />

## Component props

<PropsTable component="Blockquote" />
