import { CheckboxIndeterminateDemo } from './indeterminate';
import { CheckboxSizesDemo } from './sizes';
import { CheckboxConfigurator } from './configurator';

export const CheckboxDemos = {
  __name: '@mantine/core/Checkbox',
  indeterminate: CheckboxIndeterminateDemo,
  sizes: CheckboxSizesDemo,
  configurator: CheckboxConfigurator,
};
