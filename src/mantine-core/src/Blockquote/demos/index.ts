import { BlockquoteConfigurator } from './configurator';
import { BlockquoteIconDemo } from './icon';
import { BlockquoteNoIconDemo } from './noIcon';
import { BlockquoteBaseDemo } from './base';

export const BlockquoteDemos = {
  __name: '@mantine/core/Blockquote',
  configurator: BlockquoteConfigurator,
  icon: BlockquoteIconDemo,
  noIcon: BlockquoteNoIconDemo,
  base: BlockquoteBaseDemo,
};
