import { ActionIconColorsDemo } from './colors';
import { ActionIconSizesDemo } from './sizes';
import { ActionIconRadiusDemo } from './radius';
import { ActionIconVariantsDemo } from './variants';
import { ActionIconConfigurator } from './configurator';
import { ActionIconUsageDemo } from './usage';

export const ActionIconDemos = {
  __name: '@mantine/core/ActionIcon',
  colors: ActionIconColorsDemo,
  sizes: ActionIconSizesDemo,
  radius: ActionIconRadiusDemo,
  variants: ActionIconVariantsDemo,
  configurator: ActionIconConfigurator,
  usage: ActionIconUsageDemo,
};
