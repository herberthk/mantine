import { AlertTitleDemo } from './title';
import { AlertConfigurator } from './configurator';
import { AlertBasicDemo } from './basic';

export const AlertDemos = {
  __name: '@mantine/core/Alert',
  title: AlertTitleDemo,
  configurator: AlertConfigurator,
  basic: AlertBasicDemo,
};
